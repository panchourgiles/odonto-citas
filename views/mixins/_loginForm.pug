mixin loginForm()
  form.form(action="/login" method="POST")
    h2 Login
    label(for="email") Email
    input(type="email" name="email")
    label(for="password") Contraseña
    input(type="password" name="password")
    input.button(type="submit" value="Entrar")
